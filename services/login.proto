syntax "proto3"
package services;

import "google/api/annotations.proto";
import "stars-app/messages"


service LoginService {
  rpc Login(User) returns (User) {
    option (google.api.http) = {
      post: "/v1/login"
      body: "*"
    };
  }
}
